{
  "name": "datagrid-lit-example",
  "version": "1.0.0",
  "description": "Example of DataGrid using Lit and TypeScript",
  "main": "index.js",
  "scripts": {
    "build": "tsc && rollup -c",
    "dev": "tsc --watch",
    "serve": "web-dev-server --node-resolve --open"
  },
  "dependencies": {
    "lit": "^3.0.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.0.0",
    "@web/dev-server": "^0.4.0",
    "rollup": "^4.0.0",
    "typescript": "^5.0.0"
  }
}